#!/bin/bash -e

# Garante que o banco de dados seja criado/atualizado em todos os deploys
# independente do comando que o Render usar para subir a máquina
bundle exec rails db:prepare

# Inicia o processo principal
exec "${@}"
